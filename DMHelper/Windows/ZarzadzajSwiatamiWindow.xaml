<Window x:Class="DMHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DMHelper"
        mc:Ignorable="d"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="Transparent"
        AllowsTransparency="True"
        Height="600" Width="800">
    <Border Background="#222222"
            CornerRadius="20">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <StackPanel>
                <TextBlock Foreground="White"
                           FontSize="25"
                           Margin="20,20,10,20">Lista światów:</TextBlock>
                <ScrollViewer Margin="40,10,40,10">
                    <Button Style="{StaticResource ListButton}"
                    Content="Nazwa świata 1"/>
                </ScrollViewer>
            </StackPanel>
        </Grid>
    </Border>
</Window>
